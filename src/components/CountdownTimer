import React from 'react';

interface CountdownTimerProps {
  seconds: number;
}

export const CountdownTimer: React.FC<CountdownTimerProps> = ({ seconds }) => {
  return (
    <div style={{ fontSize: '48px', fontWeight: 'bold', color: '#03dac6' }}>
      {seconds}s
    </div>
  );
};
